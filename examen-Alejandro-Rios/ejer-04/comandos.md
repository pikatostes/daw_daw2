Copiamos el contenido del archivo docker-compose.yml en el archivo local
```bash
curl -sSL https://raw.githubusercontent.com/bitnami/containers/main/bitnami/moodle/docker-compose.yml > docker-compose.yml
```

Ejecutamos
```bash
docker compose up -d
```